using System;


namespace Diffrences
{
    class Diffrences
    {
        static void Main(string[] args)
        {
            Console.WriteLine(" What is the difference between .NET and C#?\n\n *C# is a programming language and .NET is the framework that the language is built on.\n *.NET is a blanket term that tends to cover both the .NET Framework (an application framework library) and the Common Language Runtime in which .NET assemblies are run.\n *If a job requires .NET knowledge, probably it need a programmer who knows the entire set of languages provided by the .NET framework.");


        }
    }
}
